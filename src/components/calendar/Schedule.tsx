import React from "react";
import { styled } from "@mui/material/styles";

export const Schedule = () => {
  return <SDiv sx={{ bgcolor: "darkseagreen", color: "#fff" }}>Schedule</SDiv>;
};

const SDiv = styled("div")``;
